# EngineeringOS CLI

Typer-based command-line interface for the AI Company Framework. Entry point: `engineeringos`.

All commands delegate to the Framework API in `company_core` — the CLI contains no SDLc or validation business logic.
