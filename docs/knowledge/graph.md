# Knowledge Graph

Relationships: `depends_on`, `implements`, `supersedes`, `duplicates`, `contradicts`, `references`, `caused_by`, `derived_from`, `verified_by`, `owned_by`, `related_to`.

```bash
engineeringos knowledge graph kn-abc123 --depth 2
```
